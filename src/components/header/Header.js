import React from 'react'

//import { Link } from 'react-router-dom'

import './Header.css'

//import {items} from '../../pages/home/pages'

export const Header = (props) => (     
  <div className="Header">
    {/* logo here */}
    <div className="navigation">
      {/* {items.map((places) => (
        <Link key={places.code} to={places.to} className="Card-link">
          {places.title}
        </Link>
      ))} */}
    </div>
  </div>
)
